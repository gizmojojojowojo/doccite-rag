# Export these in your shell; this file is documentation, not automatically loaded.
# Only required for --generator openai. Never commit a real key.
OPENAI_API_KEY=
DOCCITE_MODEL=
# API defaults: extractive generator, hash embeddings, loopback listener.
DOCCITE_INDEX=.doccite/index.sqlite
DOCCITE_GENERATOR=extractive
